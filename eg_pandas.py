import pandas as pd

# ---------------------------------------------------------
# 1. DataFrame (표 형태의 데이터 구조)
# - 여러 numpy 배열(column)을 묶은 2차원 구조
# - 통계 및 집계 기능 내장 (sum, mean, describe 등)
# - CSV, Excel, SQL 등 외부 데이터 입출력 가능
# ---------------------------------------------------------

# 2. dictionary type
# - Python 내장 자료형 (key:value)
# - DataFrame 생성 시 입력 형태로 자주 사용
# - Pandas 외부에서는 통계기능 없음 (단순 데이터 저장용)
# ---------------------------------------------------------

# 예제 데이터: 학생별 교과목 점수
data = {
  "name": ["Alice", "Bob", "Charlie", "David"],
  "math": [95, 82, 78, 91],
  "english": [88, 90, 67, 85],
  "science": [92, 84, 73, 89]
}

# DataFrame 생성
df = pd.DataFrame(data)

# ---------------------------------------------------------
print("📘 원본 데이터")
print(df)

# ---------------------------------------------------------
print("\n📘 예제 1: 총점(total)과 평균(avg) 열 추가")

# 새 열 추가 (열 단위 계산)
df["total"] = df["math"] + df["english"] + df["science"]
df["avg"] = df["total"] / 3

print(df)

# ---------------------------------------------------------
print("\n📘 예제 2: 수학 점수가 90점 이상인 학생 필터링")

# 조건 필터링 (True/False 인덱싱)
high_math = df[df["math"] >= 90]
print(high_math)

# ---------------------------------------------------------
print("\n📘 예제 3: 기본 통계 요약 (describe)")
print(df.describe())
